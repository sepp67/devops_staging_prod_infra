- name: Restart monitoring stack
  community.docker.docker_compose_v2:
    project_src: "{{ monitoring_install_dir | default('/opt/monitoring') }}"
    files: ["compose.yml"]
    state: present
    restarted: true
