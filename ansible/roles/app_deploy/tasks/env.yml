---
- name: Render .env file
  ansible.builtin.template:
    src: env.j2
    dest: "{{ app_install_dir }}/{{ app_env_filename }}"
    owner: "{{ app_user }}"
    group: "{{ app_group }}"
    mode: "0640"
