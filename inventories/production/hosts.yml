---
all:
  children:

    proxy:
      hosts:
        vm-proxy-prod:
          ansible_host: 10.20.1.10

    apps:
      hosts:
        vm-apps-prod:
          ansible_host: 10.20.1.11

    nextcloud:
      hosts:
        vm-nextcloud-prod:
          ansible_host: 10.20.1.12

    # Aucune VM caméra de production n'existe à ce jour. Le groupe est
    # déclaré vide plutôt que de contenir une IP placeholder — une IP
    # fictive serait indistinguable d'une vraie machine dans six mois.
    # Ajouter le host réel ici (ansible_host: <IP>) le jour où une caméra
    # de production est effectivement installée, puis ajouter "webcam" à
    # active_projects dans group_vars/all/main.yml.
    webcam:
      hosts: {}
