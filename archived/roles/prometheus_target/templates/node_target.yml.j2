- targets:
    - {{ prometheus_target_address | to_json }}
  labels:
{% for key, value in (prometheus_effective_labels | combine(prometheus_target_labels, recursive=True)) | dictsort %}
    {{ key }}: {{ value | to_json }}
{% endfor %}
