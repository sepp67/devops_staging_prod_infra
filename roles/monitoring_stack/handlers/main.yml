---
- name: Restart monitoring stack
  community.docker.docker_compose_v2:
    project_src: "{{ monitoring_stack_root_dir }}"
    files:
      - "{{ monitoring_stack_compose_path }}"
    state: present
    pull: always
    recreate: auto
