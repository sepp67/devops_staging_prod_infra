---
# nextcloud_stack — À implémenter
# Déploie Nextcloud + PostgreSQL + Redis avec volumes persistants.
# Ce rôle est distinct de docker_app : ne pas le remplacer par docker_app.
- name: nextcloud_stack placeholder
  debug:
    msg: "nextcloud_stack not yet implemented"
