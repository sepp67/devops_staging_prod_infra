---
caddy_version: "2.8.4"
caddy_config_dir: /etc/caddy
caddy_caddyfile: /etc/caddy/Caddyfile
caddy_data_dir: /var/lib/caddy
caddy_log_dir: /var/log/caddy

# Injectés depuis group_vars :
# caddy_tls_mode: internal | acme
# caddy_email: ""  (requis seulement en acme)
# projects: []     (construit par deploy-proxy.yml)
# backend_host, nextcloud_backend_host, nextcloud_domain, nextcloud_host_port
